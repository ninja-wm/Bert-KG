### use # to comment out the configure item

################ Status ################
mode=interactive_predict
# string: train/interactive_predict

################ Datasets(Input/Output) ################
datasets_fold=data/mydataset
train_file=myData.json
dev_file=myData.json
test_file=None

################ Dir ################
log_dir=logs
checkpoints_dir=models/mydataset

################ Labeling Scheme ################

class_name=[父本,母本,表型,环境]
decision_threshold=0.5

################ Training Settings ################
max_sequence_length=600

epoch=50
batch_size=32

dropout=0.5
learning_rate=5e-5

is_early_stop=True
patient=5
# unnecessary if is_early_stop=False